package org.alexisjago.videostream.stream

class RedisStreamService extends StreamService {
  override def getStreams(id: Int): Unit = ???

  override def startStream(id: Int): Unit = ???

  override def stopStream(id: Int): Unit = ???
}
